import { atom } from 'jotai'
import { atomWithStorage } from 'jotai/utils'

export const modalCreate{{pascalCase name}}Atom = atom<boolean>(false)

export const modalUpdate{{pascalCase name}}Atom = atom<boolean>(false)

export const current{{pascalCase name}}IDAtom = atom<string>('')

export const modal{{pascalCase name}}DeleteAtom = atom<boolean>(false)
