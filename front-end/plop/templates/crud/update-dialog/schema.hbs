// Schema não utilizado no padrão atual
// O UpdateTesteDialog usa react-hook-form diretamente sem Zod
// Se precisar de validação com Zod, descomente e ajuste conforme necessário

/*
import { z } from 'zod'

export const update{{pascalCase entityName}}Schema = z.object({
  name: z.string().min(1, 'Nome é obrigatório').max(255, 'Nome deve ter no máximo 255 caracteres'),
})

export type Update{{pascalCase entityName}}FormData = z.infer<typeof update{{pascalCase entityName}}Schema>
*/
