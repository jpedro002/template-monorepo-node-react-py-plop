import { z } from 'zod'

export const {{camelCase entityName}}FilterSchema = z.object({
  searchTerm: z.string().optional(),
})

export type {{pascalCase entityName}}FilterFormData = z.infer<typeof {{camelCase entityName}}FilterSchema>
