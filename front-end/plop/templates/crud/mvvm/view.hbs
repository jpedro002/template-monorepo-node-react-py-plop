import { Pagination } from './components/Pagination'
import { Create{{pascalCase entityName}}Dialog } from './components/create-{{kebabCase entityName}}-dialog'
import { Filter } from './components/filter'
import { Table{{pascalCase entityName}} } from './components/table-{{kebabCase entityName}}'
import { Update{{pascalCase entityName}}Dialog } from './components/update-{{kebabCase entityName}}-dialog'
import { {{pascalCase entityName}}ViewProps } from './types'

export function {{pascalCase entityName}}View(props: {{pascalCase entityName}}ViewProps) {
	const { data, onEdit, viewMode, setViewMode } = props

	return (
		<main className="flex-1 max-w-full w-full overflow-hidden">
			<div className="container mx-auto p-4 space-y-6 sm:p-6 md:p-8">
				<div className="space-y-4 mb-6">
					<div className="flex flex-col sm:flex-row sm:justify-between sm:items-center gap-4">
						<div>
							<h2 className="text-xl sm:text-2xl font-bold tracking-tight">
								Gerenciar {{titleCase entityName}}s
							</h2>
							<p className="text-sm sm:text-base text-muted-foreground">
								Gerencie todos os {{lowerCase entityName}}s do sistema
							</p>
						</div>

						<Create{{pascalCase entityName}}Dialog />
					</div>
				</div>
				<Filter />
			</div>

			<Table{{pascalCase entityName}} onEdit={onEdit} {{camelCase entityName}}s={data.data} />

			<div className="container mx-auto p-4 sm:p-6 md:p-8">
				<Update{{pascalCase entityName}}Dialog />
				<Pagination
					page={data.pagination.page}
					rowCount={data.pagination.rowCount}
					pageCount={data.pagination.pageCount}
					pageSize={data.pagination.pageSize}
				/>
			</div>
		</main>
	)
}